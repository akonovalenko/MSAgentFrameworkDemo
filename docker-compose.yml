version: "3.8"
services:
  bitcoinagent-api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CONFIGURATION: Release
    image: bitcoinagent:local
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped